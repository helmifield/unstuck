# UNSTUCK iOS — local development environment example.
#
# NON-SECRET template. Xcode reads these via the "unstuck-local" configuration's
# xcconfig (see apps/iOS/Unstuck/Configuration/Environment.swift). Copy to a
# gitignored `.env`-style file if needed; never commit real secrets or Apple
# private keys (.p8) — they are gitignored and excluded from CI.
#
# Use placeholder/local hosts only; no production endpoints.

API_BASE_URL=http://127.0.0.1:3000
